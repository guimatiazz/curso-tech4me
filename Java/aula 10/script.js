//criar um programa, onde voce escrevam o nome dde cada aluno da turma 
//mostrar o nome de todos no console.log

const nomes = new Array(7);

function axibirArray() {
    for (let index = 0; index < nomesCadrastrados; index++) {
        alert(nomes[index]);
    }
}

let 